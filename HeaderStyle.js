import { StyleSheet } from 'react-native';

const headerStyle = StyleSheet.create({
    header: {
        textAlign: 'center',
        fontSize: 40,
        color: '#f44242',
        padding: 50,
        fontWeight: '300'
    },
    iOSheader: {
        textAlign: 'center',
        fontSize: 20,
        color: '#f44242',
        padding: 50,
        fontWeight: '300'
    }
});

export default headerStyle;